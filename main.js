// Main entry point for the application
document.addEventListener('DOMContentLoaded', () => {
    console.log('Main script loaded');
    
    // Transport controls and Add Track button have been removed
    
    // We'll keep only the track creation dialog functionality
    // The dialog will be triggered by the "Add Track" button in trackCreation.js
    // which appears to be in a different location than the one we're removing
}); 